# Test dependencies — not needed in production
pytest>=8.0
pytest-httpx>=0.30        # mock httpx calls (LLM client)
httpx>=0.27               # required by pytest-httpx
beautifulsoup4>=4.12      # parse HTML in frontend tests
# Production deps (also needed to run tests)
fastapi>=0.111
uvicorn>=0.30
python-docx>=1.1
PyMuPDF>=1.24
pydantic>=2.7
httpx>=0.27
python-multipart>=0.0.9   # required by FastAPI for UploadFile / Form data
